## produce muts from each pool. also generate histogram of allele freq. (outliers then will be calculated in counts)

source("../../script/count_mut_pool.R")


count_mut_and_plot_all("sample_seq_info_200721.txt")
